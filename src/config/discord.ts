// Discord application (client) ID for the CFB Companion Activity.
// This value is public — it appears in the app's invite/authorize URL — so it
// is safe to commit. If you ever want to configure it per-environment, move it
// to a NEXT_PUBLIC_DISCORD_CLIENT_ID env var and read it here instead.
export const DISCORD_CLIENT_ID = '1520603855543337141';
